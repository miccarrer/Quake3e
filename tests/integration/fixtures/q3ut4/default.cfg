// Minimal default.cfg so the dedicated server can boot headless without a real
// game install (no pk3s required). The engine fatally errors if default.cfg is
// missing or empty, so keep at least one statement here.
set com_introPlayed 1
